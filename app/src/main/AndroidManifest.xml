<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectapp">

    <uses-permission-sdk-23 android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SNK"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".CalendarActivity2"
            android:label="@string/title_activity_calendar2"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".CalendarsActivity" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".Donators" />
        <activity android:name=".Admin" />
        <activity android:name=".DonationList" />
        <activity android:name=".NewDonation" />
        <activity android:name=".LogIn" />
        <activity android:name=".Registration" />
        <activity
            android:name=".MainActivity"
            android:label="SNK"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="com.facebook.FacebookContentProvider"
            android:authorities="com.facebook.app.FacebookContentProvider798692694220646"
            android:exported="true" />
    </application>

</manifest>